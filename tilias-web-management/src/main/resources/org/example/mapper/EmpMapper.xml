<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.EmpMapper">

<!--    根据id更新员工基本信息-->
<!--    set标签:会自动生成set关键字,也会自动删除多余的 ","     而where标签会删除多余的and-->
    <update id="updateById">
        UPDATE emp
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="job != null and job != ''">
                job = #{job},
            </if>
            <if test="salary != null">
                salary = #{salary},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="entryDate != null">
                entry_date = #{entryDate},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>


    <!--    批量删除员工的基本信息-->
    <delete id="deleteByIds">
        delete from emp where id in
            <foreach collection="ids" item="id" separator="," close=")" open="(">
                #{id}
        </foreach>
    </delete>

    <!--    这个id指的应该是关于要执行函数的函数名的,tyoe应该是要返还的数据类型 #concat('%',#{name},'%')用来进行字符串拼接-->
<!--    你 where标签可以自动删除and和or之类的连接,不会出现 空 and ...的情况-->
<!--    if标签进行判断是否需要执行-->


    <select id="list" resultType="org.example.pojo.Emp">

        select emp.*,dept.name deptName from emp left join dept on emp.dept_id = dept.id
        <where>
            <if test="name != null and name != '' ">
                emp.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and emp.gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and emp.entry_date between #{begin} and #{end}
            </if>
        </where>
        order by emp.update_time desc

    </select>

<!--根据员工id查询员工基本信息及其工作经历-->
    <resultMap id="empResultMap" type="org.example.pojo.Emp">
        <id column="id" property="id"/>
        <result column = "username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
        <result column="job" property="job"/>
        <result column="salary" property="salary"/>
        <result column="image" property="image"/>
        <result column="entry_date" property="entryDate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

        <collection property="exprList" ofType="org.example.pojo.EmpExpr">
            <id column="ee_id" property="id"/>
            <id column="ee_empid" property="empId"/>
            <id column="ee_begin" property="begin"/>
            <id column="ee_end" property="end"/>
            <id column="ee_company" property="company"/>
            <id column="ee_job" property="job"/>
        </collection>
    </resultMap>

<!--    根据Id查询返回员工的基本信息-->
    <select id="getinfo" resultMap="empResultMap">
        select emp.*,
               ee.id ee_id,
               ee.emp_id ee_empid,
               ee.begin ee_begin,
               ee.end ee_end,
               ee.company ee_company,
               ee.job ee_job
               from emp left join emp_expr ee on emp.id = ee.emp_id
               where emp.id = #{id}
    </select>

    <select id="countEmpJobData" resultType="java.util.Map">
        select
            (case when job = 1 then '班主任'
                  when job = 2 then '讲师'
                  when job = 3 then '学工主管'
                  when job = 4 then '教研主管'
                  when job = 5 then '咨询师'
                  else '其他' end) pos
            ,count(*) num
        from emp group by job order by num
    </select>

    <select id="countEmpGenderData" resultType="java.util.Map">
        select
            if(gender = 1 ,'男性员工','女性员工') name,
            count(*) value
        from emp group by gender
    </select>

<!--    查询所有员工接口-->
    <select id="jieKou" resultType="org.example.pojo.Emp">
        select * from emp
    </select>

<!--    登录验证功能的实现-->
    <select id="LoginInfo" resultType="org.example.pojo.LoginInfo">
        select e.id,e.username,e.name  from emp e where e.username = #{username} and e.password = #{password}
    </select>


</mapper>